# ADR

## ADR-0001 — UI 프레임워크를 AdminLTE 3 / Bootstrap 4 에서 Bootstrap 5.3 으로 옮긴다

**상태**: Accepted (2026-09-25)

**맥락**

현재 레이아웃은 AdminLTE 3.2.0 + Bootstrap 4.6.2 + jQuery 3.6.0 이다. AdminLTE 는 관리자 대시보드용이고 이 사이트는 공개 안내 사이트라, 사이드바·컨트롤바·박스 위젯 등 쓰지 않는 것이 대부분이다. 실제로 `main.css` 는 AdminLTE 가 `.wrapper` / `.content-wrapper` 에 박아둔 배경을 `!important` 로 되돌리는 데 지면을 쓰고 있다. Bootstrap 4 는 2023-01 EOL 이고 다크 모드 개념이 아예 없어서, 드롭다운·탭·alert·표의 다크 대응을 전부 손으로 짜야 한다. 지금 다크가 절반쯤 깨져 있는 근본 원인이 이것이다.

**결정**

Bootstrap 5.3 의 color modes (`data-bs-theme`) 로 옮기고 AdminLTE 와 jQuery 를 걷어낸다. 5.3 은 모든 컴포넌트가 라이트/다크를 내장하고 있어, 지금 손으로 짠 테마 토글 스크립트도 공식 문서 스니펫으로 대체된다.

**근거**

- 컴포넌트 다크 대응이 프레임워크 몫으로 넘어간다 — 이 작업의 목적 자체다.
- jQuery / AdminLTE 두 개의 런타임 의존이 사라진다. 현재 CDN 4곳에 의존하는데 그중 둘이 없어진다.
- 클래스명 변환은 기계적이다: `ml-*`→`ms-*`, `mr-*`→`me-*`, `float-right`→`float-end`, `data-toggle`→`data-bs-toggle`, `text-muted`→`text-body-secondary`.
- 3개로 복붙된 다크 팔레트 블록(`main.css:35`, `:356`, `:381`)이 하나로 정리된다.

**버리는 것**

AdminLTE 의 navbar 스타일에 기대던 부분은 다시 짜야 한다. 그러나 현재도 `main.css` 에서 대부분 덮어쓰고 있어 실질 손실은 작다.

**대안**

- *Bootstrap 4 유지 + 다크 직접 보수* — 지금의 반복이다. EOL 프레임워크에 손으로 짠 테마를 계속 얹는 셈이라 기각.
- *Pico CSS (클래스리스, 자동 라이트/다크)* — 가볍지만 탭·드롭다운을 직접 만들어야 한다. 접속 안내의 OS 탭이 이 사이트의 핵심 UI 라 기각.
- *Tailwind* — 빌드 파이프라인이 붙는다. Jekyll + GitHub Pages 의 무빌드 장점을 잃어 기각.

---

## ADR-0002 — 배색은 Radix Colors 스케일을 그대로 가져다 쓴다

**상태**: Superseded by ADR-0004 (2026-09-25)

**맥락**

현재 라이트 팔레트는 생성된 값이고 다크는 급조된 값이라, 양쪽 모두 명암비가 설계되지 않았다. 색을 하나 고칠 때마다 라이트/다크 두 벌을 각각 눈으로 맞춰야 하는데 이게 지속되지 않는다. 직접 팔레트를 설계하는 대신, **라이트/다크가 쌍으로 검증된 스케일을 그대로 쓴다**는 것이 이 결정의 전제다.

**결정**

Radix Colors 의 12단계 스케일을 vendor 로 받아 토큰의 출처로 삼는다. 중립은 `slate`, 브랜드 계열은 `blue` 를 쓴다. Bootstrap 5.3 의 시맨틱 변수를 이 스케일에 매핑하고, 사이트 코드는 Bootstrap 변수만 참조한다.

Radix 스케일은 단계마다 역할이 정해져 있고 라이트/다크에서 **같은 단계가 같은 역할**을 하도록 설계되어 있다. 이게 "한 번에 해결"의 실체다.

| 단계 | 역할 | 매핑할 Bootstrap 변수 |
|---|---|---|
| 1–2 | 페이지 / 카드 배경 | `--bs-body-bg`, 카드 배경 |
| 3–5 | 컴포넌트 배경, hover | `--bs-tertiary-bg` |
| 6–8 | 테두리 | `--bs-border-color` |
| 9–10 | 솔리드 브랜드 색 | `--bs-primary` |
| 11 | 보조 텍스트 (배경 위 4.5:1 보장) | `--bs-secondary-color` |
| 12 | 본문 텍스트 | `--bs-body-color` |

**근거**

- 명암비가 스케일 자체에 설계되어 있다. `#888` / `#aaa` / `#444` 를 눈으로 고르던 것을 그만둔다.
- 다크 스케일이 라이트와 짝으로 제공되므로 다크를 따로 설계하지 않는다.
- 순수 CSS 커스텀 프로퍼티 파일이라 빌드 도구가 필요 없다.

**구현 제약**

- **`light-dark()` CSS 함수를 쓰지 않는다.** 이 함수는 Chromium 123 / Safari 17.5 이상이라 구버전 Samsung Internet 에서 동작하지 않는다. 이 개편의 출발점이 삼성 단말 다크 이슈인데 정작 삼성 브라우저에서 안 먹는 문법을 쓰면 본말전도다. `[data-bs-theme="dark"]` 선택자 아래에서 변수를 재정의하는 방식으로 간다 — 전 브라우저에서 동작한다.
- **hex 값을 기억으로 적지 않는다.** Radix CSS 파일을 실제로 받아서 넣는다 (CLAUDE.md "숫자는 측정해서 쓴다").
- CDN 이 아니라 `assets/css/vendor/` 에 커밋한다 (CLAUDE.md CDN 정책).
- eduroam 브랜드 네이비(`#003d7a`)는 라이트 navbar/히어로의 **면 색**으로만 남긴다. 텍스트·아이콘 강조색으로 쓰던 자리는 `blue-11` 로 옮긴다 — 네이비는 다크 카드 위에서 명암비 1.43 으로 읽히지 않는다.

**대안**

- *Bootstrap 5.3 기본 팔레트만 사용* — 결정 수는 가장 적지만 "배색을 팍 바꾼다"는 요구에 못 미치고, 브랜드 계열 색은 여전히 직접 골라야 한다.
- *Open Props* — 라이트/다크 쌍 보장이 Radix 만큼 명시적이지 않다.
- *USWDS / GOV.UK 팔레트* — 공공 영역 검증은 강하지만 다크 토큰이 완결되어 있지 않다.

---

## ADR-0003 — 반응형은 Bootstrap 그리드에만 맡기지 않는다

**상태**: Accepted (2026-09-25)

**맥락**

`assets/css/main.css` 에 반응형 미디어 쿼리가 **0개**다. `@media` 는 `prefers-color-scheme` 두 개뿐이고, 화면 폭 대응은 전부 마크업의 `col-6 col-md-3` 같은 그리드 클래스에 의존한다. 그래서 폭이 좁아져도 카드 패딩(32px), 폰트 크기, 섹션 여백이 데스크탑 값 그대로다. 360px 화면에서 본문 가용 폭이 270px 남짓이 되는 이유다.

**결정**

`sm` / `md` 구간 미디어 쿼리를 도입해 패딩·타이포 스케일을 낮춘다. 한국어 사이트이므로 `word-break: keep-all` 을 전역에 넣어 어절 중간 줄바꿈을 막는다.

**근거**

그리드 클래스는 열 배치만 바꾼다. 여백과 글자 크기는 그리드가 모르는 영역이고, 지금은 아무도 안 보고 있다.

---

## ADR-0004 — Radix *Themes* 토큰 층을 쓴다. 스케일 매핑조차 직접 하지 않는다

**상태**: Accepted (2026-09-25). ADR-0002 를 대체한다.

**맥락**

ADR-0002 대로 Radix Colors 를 vendor 해서 썼고, 받아온 CSS 는 업스트림과 바이트 단위로 동일했다. 그런데도 "검증된 스킴을 변형 없이"라는 요구는 절반만 충족됐다. Radix **Colors** 는 의도적으로 스케일만 주고 "몇 번 단계를 어디에 쓸지"는 쓰는 사람이 정하게 되어 있기 때문이다. 그 매핑은 사람이 한 판단이고, 실제로 그 층에 스케일 밖 값이 섞여 들어갔다 — eduroam 네이비 `#003d7a`, 솔리드 위 흰 글자, 그림자 rgba, 히어로 그라디언트의 중간 스톱.

그중 두 개는 실제로 틀렸다. 솔리드에 `blue-9` + 흰 글자는 3.26 이었고, 옛 히어로 그라디언트의 밝은 끝은 흰 글자조차 4.23 이었다. 즉 "어느 단계를 어디에" 는 눈으로 정할 수 있는 문제가 아니다.

**결정**

`@radix-ui/themes` 의 `tokens.css` 를 쓴다. 컴포넌트 CSS(`styles.css`)는 쓰지 않는다 — Bootstrap 과 싸운다.

이 파일은 스케일 위에 **시맨틱 층**까지 들어 있다.

| 토큰 | 뜻 |
|---|---|
| `--color-background` | 페이지 배경 |
| `--color-panel-solid` | 카드·패널 면 |
| `--accent-9` | 솔리드 강조 면 |
| `--accent-contrast` | **그 면 위에 올려도 되는 글자색** |
| `--gray-11` / `--gray-12` | 보조 텍스트 / 본문 텍스트 |
| `--shadow-1..6`, `--radius-1..6` | 그림자, 모서리 |

`--accent-contrast` 가 핵심이다. 솔리드 배경 위 글자색을 더 이상 사람이 고르지 않는다.

강조색은 `<html data-accent-color="…">` 속성 하나로 바뀌고, 고를 수 있는 값이 26종이다. `_config.yml` 의 `theme.accent` / `theme.gray` 로 노출했으므로 fork 한 기관이 자기 색으로 바꿀 때 CSS 를 열 필요가 없다. 현재 값은 `indigo` + `slate`.

**결과**

`assets/css/main.css` 에 hex 와 rgba 가 **0개**다. 색은 전부 토큰 참조다. 이 파일에 hex 가 하나라도 생기면 되돌려야 할 변경으로 본다.

측정 결과도 바뀌었다. 히어로·navbar 의 그라디언트를 솔리드로 바꾸면서, 자동 검사가 색을 계산할 수 없어 "확인 불가"로 빠지던 10종이 **0종**이 됐다. 7페이지 × 6조합의 모든 텍스트/배경 쌍이 이제 측정 대상이고 미달 0건이다.

**대가**

`tokens.css` 는 34 KB (gzip) 다. 26개 강조색 전부와 그림자·모서리 토큰이 들어 있어서 그렇고, 실제로 쓰는 건 그중 하나다. 색 하나만 쓰겠다고 파일을 잘라내면 그 순간 "변형 없이"가 깨지므로 자르지 않는다. 용량이 문제가 되면 vendor 전체를 재검토하는 자리에서 같이 다룬다.

**한 가지 남는 판단**

navbar·히어로·푸터를 양쪽 테마 모두 `--accent-9` 솔리드로 둔 것은 취향 판단이다. 다크에서 채도 높은 면이 그대로 남는다 — 이전 리뷰가 지적했던 그 인상이다. 다만 지금은 `--accent-contrast` 로 가독성이 보장된 상태이고, 브랜드 면이 테마에 따라 색을 바꾸지 않는 쪽이 일관적이라고 보아 유지했다. 다크에서 가라앉히길 원하면 `--nav-bg` 를 `var(--gray-2)` 로 바꾸는 한 줄이다.

---

## ADR-0005 — 공지 본문은 한국어만 유지한다

**상태**: Accepted (2026-09-25)

**맥락**

`/en/` 트리는 있고 공지 목록도 `title_en` 으로 영문 제목을 보여준다. 그런데 상세로 들어가면 본문이 한국어다 (OBS-20260925-02). `nro-site` 는 `_notices_en/` 컬렉션과 `notice_en.html` 레이아웃을 따로 두는 방식으로 풀었다.

**결정**

본문은 한국어만 유지한다. `_notices_en/` 을 만들지 않는다.

**근거**

한국 기관 이용자를 위한 사이트다. 공지는 점검 일정·약관 개정처럼 국내 이용자에게 가는 내용이고, 이걸 이중화하면 공지를 올릴 때마다 번역이 선행 조건이 된다 — 그러면 공지가 늦거나, 한쪽이 비거나, 둘 다 된다. 영문 트리의 역할은 방문자에게 접속 방법과 정책을 안내하는 것까지이고 거기까지는 이미 번역되어 있다.

목록에 영문 제목이 나오는 것은 유지한다. 무슨 공지가 있었는지는 영문 사용자도 알 수 있어야 하고, `title_en` 한 줄은 유지 비용이 낮다.

**뒤집는다면**

영문 사용자 문의가 실제로 들어오거나 NRO 가 국제 대상 공지를 내보내야 할 때. 그때는 `nro-site` 방식이 이미 검증된 참고 구현이다.

---

## ADR-0006 — 제목바는 강조색 면 + 단색 흰 로고로 간다

**상태**: Proposed (2026-09-25)

**맥락**

지금 navbar 브랜드는 Font Awesome wifi 아이콘 + "기관명 eduroam" 텍스트다. 저장소에 실제 eduroam 로고가 컬러 SVG (`eduroam-logo.svg`) 와 흰색 PNG (`eduroam-logo-white.png`) 두 벌 들어 있는데 아무 데서도 쓰이지 않는다.

참고한 실물 둘. **eduroam.gr** 은 흰 바 + 컬러 로고, **eduroam.org** 는 네이비 바 + 흰 로고다.

네 가지를 실제로 렌더해서 라이트/다크로 비교했다.

**결정**

강조색 면(`--accent-9`) + 단색 흰 로고 + 구분선 + 기관명. eduroam.org 쪽이다.

**근거 — 취향이 아니라 두 가지 제약에서 나온다**

- **컬러 로고는 팔레트를 묶는다.** eduroam 로고의 고정색은 `#00658A` (틸) 과 `#1B1816` 이다. `theme.accent` 가 `indigo` 인 지금 상태로 흰 바에 올리면 성격이 다른 파랑 둘이 나란히 선다 — 렌더에서 바로 보인다. 컬러 로고를 쓰려면 강조색을 `cyan` / `teal` 근처로 고정해야 하고, 그러면 ADR-0004 에서 확보한 "26종 중 아무거나" 가 사라진다. 흰 로고는 단색이라 어떤 강조색 위에도 올라간다.
- **컬러 로고는 다크에서 분기를 되살린다.** `#1B1816` 은 어두운 바에서 안 보이므로 다크에서는 흰 로고로 갈아끼워야 하고, 바도 `--gray-2` 로 내려야 한다. 그런데 히어로는 `--accent-9` 라서, 다크에서 검은 바 아래 채도 높은 히어로가 붙는 모양이 된다 — 이것도 렌더에서 보인다. 맞추려면 히어로까지 테마별로 갈라야 하는데, ADR-0004 에서 없앤 분기가 그대로 돌아온다.

흰 로고를 쓰면 바와 히어로가 두 테마 모두에서 같은 면으로 이어지고, 테마별 분기가 0 으로 유지된다.

**뒤집는 조건**

기관 로고를 **원색 그대로** 나란히 보여야 한다면 (Radix 사이트의 `Radix | Made by WorkOS` 같은 배치) 흰 바가 맞다. 기관 로고의 단색 버전을 매번 요구할 수는 없기 때문이다. 그 경우 강조색을 eduroam 틸 계열로 고정하고, 히어로도 밝은 면으로 같이 내린다.

이건 템플릿이라 기관들이 그대로 복사한다. 기관 로고를 다루는 방식이 정해지면 그때 다시 본다.
